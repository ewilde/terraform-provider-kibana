builds:
  - binary: terraform-provider-kibana_v{{.Version}}
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
changelog:
  filters:
    exclude:
    - "^Merge pull request"
    - "^Merge branch"
archives:
  - format: zip
